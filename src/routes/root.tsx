/**
 * Root route, the default route for the application.
 * All other routes are nested within this route.
 * @returns // TODO: Add return
 */
export default function Root() {
    console.log("TODO: Add Root component");
    return (
        <div>
            <h1>Root</h1>
        </div>
    );
}