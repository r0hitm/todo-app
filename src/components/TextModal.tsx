/**
 * TextModal reusable component
 * Generic modal that has a text input and submit button, used to create/edit new lists
 * @param // TODO: Add props
 * @returns // TODO: Add return
 */
export default function TextModal(/*{
    submitHandler,
    cancelHandler,
    defaultValues,
 }*/) {
    console.log("TODO: Add TextModal component");
    return (
        <div>
            <h1>TextModal</h1>
        </div>
    );
}
